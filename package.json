{
  "name": "distra-rewrite",
  "private": true,
  "scripts": {
    "bootstrap": "bun scripts/bootstrap-dev.ts",
    "dev": "bun --filter \"distra-*\" dev",
    "db:migrate": "bun --filter \"distra-*\" db:migrate"
  },
  "workspaces": [
    "app",
    "packages/*"
  ]
}
